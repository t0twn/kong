services:
  nginx-proxy-manager:
    image: 'docker.io/jc21/nginx-proxy-manager:latest'
    restart: unless-stopped
    networks:
      - kong
    volumes:
      - ./data:/data
      - ./letsencrypt:/etc/letsencrypt

networks:
  kong:
    external: true
